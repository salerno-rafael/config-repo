#Configuraçao para repositorio git
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          searchLocations: file:///Users/rafaelsalerno/Documents/workspace/config-repo/config-server/config-repo/
          
SPRING_PROFILES_ACTIVE: native     

#configuraçao para repositorio fileSystem
#spring:
#  application:
#    name: config-service
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/salerno-rafael/config-repo

server:
  port: 8888
  
security:
  basic:
    enabled: false