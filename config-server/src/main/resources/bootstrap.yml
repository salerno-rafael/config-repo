spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/salerno-rafael/config-repo
server:
  port: 8888
  
security:
  basic:
    enabled: false